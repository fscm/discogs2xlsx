[pytest]
addopts =
    --strict-config
    --strict-markers
    --cov-config=.coveragerc
norecursedirs =
    .*
    build
    dist
    CVS
    _darcs
    {arch}
    *.egg
    venv
    env
    virtualenv
testpaths = tests
required_plugins =
    pytest-mock
    pytest-cov
